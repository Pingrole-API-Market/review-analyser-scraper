{
  "actorOutputSchemaVersion": 1,
  "title": "Trustpilot Review Scraper Output",
  "description": "Scraped reviews, ratings, and company details from Trustpilot.",
  "properties": {
    "dataset": {
      "title": "Reviews Dataset",
      "description": "All scraped reviews and business data in structured JSON format.",
      "type": "string",
      "template": "{{links.apiDefaultDatasetUrl}}/items"
    },
    "exportFiles": {
      "title": "Export Files",
      "description": "Downloadable CSV, XLSX, or JSON export (when 'Export as File' is enabled).",
      "type": "string",
      "template": "{{links.apiDefaultKeyValueStoreUrl}}/keys?collection=exports"
    },
    "jsonOutput": {
      "title": "JSON Output",
      "description": "Pretty JSON summary stored in the OUTPUT key.",
      "type": "string",
      "template": "{{links.apiDefaultKeyValueStoreUrl}}/records/OUTPUT"
    }
  }
}
