{
  "actorOutputSchemaVersion": 1,
  "title": "Trustpilot Review Scraper Output",
  "type": "object",
  "description": "Scraped reviews, ratings, and company details from Trustpilot.",
  "properties": {
    "dataset": {
      "title": "Reviews Dataset",
      "description": "All scraped reviews and business data in structured JSON format.",
      "template": "{{links.apiDefaultDatasetUrl}}"
    },
    "keyValueStore": {
      "title": "JSON Output & Export Files",
      "description": "Pretty JSON output (OUTPUT key) and optional CSV/XLSX export files.",
      "template": "{{links.apiDefaultKeyValueStoreUrl}}"
    }
  }
}
